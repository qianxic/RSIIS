# GUI依赖
PySide6>=6.5.0
PySide6-Addons>=6.5.0
PySide6-Essentials>=6.5.0

# 科学计算和数据处理
numpy>=1.22.0
pandas>=1.4.0
scipy>=1.8.0

# 图像处理
Pillow>=9.0.0
opencv-python>=4.6.0
scikit-image>=0.19.0

# 遥感专用库
rasterio>=1.3.0
gdal>=3.4.0

# 机器学习（用于影像解译）
scikit-learn>=1.1.0
tensorflow>=2.10.0
torch>=1.13.0

   # 对于Windows用户，可能需要从非官方渠道安装GDAL:
   # pip install GDAL-3.4.3-cp39-cp39-win_amd64.whl
   # 
   # 对于Linux用户，通常需要先安装系统库:
   # sudo apt-get install libgdal-dev